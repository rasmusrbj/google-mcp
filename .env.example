# Transport Configuration
# Set to 'sse' for cloud deployment (Railway, Cloud Run, etc.)
# Set to 'stdio' for local Claude Desktop usage
TRANSPORT=sse

# Server Configuration
# Port for SSE transport (Railway sets this automatically)
PORT=8000

# Google OAuth Credentials
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Workspace APIs (Gmail, Drive, Docs, Sheets, Slides, Calendar, Tasks, Chat, Forms)
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URIs

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/oauth2callback

# For cloud deployment, update GOOGLE_REDIRECT_URI to your Railway URL:
# GOOGLE_REDIRECT_URI=https://your-app-name.railway.app/oauth2callback

# Token Storage
# The server stores OAuth tokens in token.json
# For cloud deployment, you may need to implement persistent storage
# (Railway ephemeral filesystem means tokens reset on restart)
